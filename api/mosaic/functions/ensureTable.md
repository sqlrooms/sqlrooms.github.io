---
url: 'https://sqlrooms.org/api/mosaic/functions/ensureTable.md'
---
[@sqlrooms/mosaic](../index.md) / ensureTable

# Function: ensureTable()

> **ensureTable**(`adapter`, `tableName`): `DataTable`

Validates that a table exists. Throws if not found.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `adapter` | [`DatabaseAiAdapter`](../type-aliases/DatabaseAiAdapter.md) |
| `tableName` | `string` |

## Returns

`DataTable`
