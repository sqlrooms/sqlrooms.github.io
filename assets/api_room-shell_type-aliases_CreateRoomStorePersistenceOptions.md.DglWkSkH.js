import{_ as t,o,c as d,a2 as s}from"./chunks/framework.jisAoZJG.js";const m=JSON.parse('{"title":"Type Alias: CreateRoomStorePersistenceOptions<TState, TPersisted, TSnapshot>","description":"","frontmatter":{"head":[["link",{"rel":"canonical","href":"https://sqlrooms.org/api/room-shell/type-aliases/CreateRoomStorePersistenceOptions.html"}]]},"headers":[],"relativePath":"api/room-shell/type-aliases/CreateRoomStorePersistenceOptions.md","filePath":"api/room-shell/type-aliases/CreateRoomStorePersistenceOptions.md"}'),a={name:"api/room-shell/type-aliases/CreateRoomStorePersistenceOptions.md"};function r(c,e,i,n,l,h){return o(),d("div",null,[...e[0]||(e[0]=[s('<div style="display:none;" hidden="true" aria-hidden="true">Are you an LLM? You can read better optimized documentation at /api/room-shell/type-aliases/CreateRoomStorePersistenceOptions.md for this page in Markdown format</div><p><a href="./../">@sqlrooms/room-shell</a> / CreateRoomStorePersistenceOptions</p><h1 id="type-alias-createroomstorepersistenceoptions-tstate-tpersisted-tsnapshot" tabindex="-1">Type Alias: CreateRoomStorePersistenceOptions&lt;TState, TPersisted, TSnapshot&gt; <a class="header-anchor" href="#type-alias-createroomstorepersistenceoptions-tstate-tpersisted-tsnapshot" aria-label="Permalink to &quot;Type Alias: CreateRoomStorePersistenceOptions\\&lt;TState, TPersisted, TSnapshot\\&gt;&quot;">​</a></h1><blockquote><p><strong>CreateRoomStorePersistenceOptions</strong>&lt;<code>TState</code>, <code>TPersisted</code>, <code>TSnapshot</code>&gt; = <a href="./RoomStorePersistenceSnapshotCodec.html"><code>RoomStorePersistenceSnapshotCodec</code></a>&lt;<code>TPersisted</code>, <code>TSnapshot</code>&gt; &amp; <a href="./RoomStorePersistenceSnapshotEquivalence.html"><code>RoomStorePersistenceSnapshotEquivalence</code></a>&lt;<code>TSnapshot</code>&gt; &amp; <code>object</code></p></blockquote><p>Options for creating controller-backed persistence for a room store.</p><p>This helper composes Zustand persist storage, room-store partialization, and <code>createPersistenceController()</code> so apps can share dirty tracking, autosave, final flush, and hydration saved-snapshot behavior without repeating glue.</p><h2 id="type-declaration" tabindex="-1">Type Declaration <a class="header-anchor" href="#type-declaration" aria-label="Permalink to &quot;Type Declaration&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Name</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>partialize()</code></td><td>(<code>state</code>) =&gt; <code>TPersisted</code></td><td>Selects the portion of full room-store state that should be persisted. When using Zustand&#39;s <code>persist</code> middleware, pass the same function to its <code>partialize</code> option so the storage receives the same <code>TPersisted</code> shape.</td></tr><tr><td><code>load()</code></td><td>() =&gt; <code>Promise</code>&lt;<code>TSnapshot</code> | <code>null</code>&gt;</td><td>Loads the latest durable snapshot, or <code>null</code> when none exists.</td></tr><tr><td><code>save()</code></td><td>(<code>snapshot</code>, <code>metadata?</code>) =&gt; <code>Promise</code>&lt;<code>void</code>&gt;</td><td>Writes a durable snapshot. Metadata carries the save reason, such as <code>setItem</code>, <code>store-change</code>, <code>autosave</code>, or <code>final-flush</code>, for adapters that want observability.</td></tr><tr><td><code>remove()?</code></td><td>(<code>name</code>) =&gt; <code>Promise</code>&lt;<code>void</code>&gt;</td><td>Removes the durable snapshot when Zustand clears persistence.</td></tr><tr><td><code>store?</code></td><td><code>StoreApi</code>&lt;<code>TState</code>&gt;</td><td>Optional Zustand store to observe directly. Provide this when persistence should track store changes independently of Zustand persist&#39;s <code>setItem</code> calls.</td></tr><tr><td><code>shouldPersistChange?</code></td><td><a href="./RoomStorePersistenceChangePredicate.html"><code>RoomStorePersistenceChangePredicate</code></a>&lt;<code>TState</code>&gt;</td><td>Returns true when a changed partialized snapshot should mark persistence dirty. Use this for host lifecycle guards, such as skipping restore-time updates or failed initialization states. Skipped snapshots are still recorded as observed so they are not saved later by an unrelated state change.</td></tr><tr><td><code>applySnapshot()?</code></td><td>(<code>persisted</code>, <code>context</code>) =&gt; <code>void</code> | <code>Promise</code>&lt;<code>void</code>&gt;</td><td>Applies a deserialized snapshot to runtime state during <code>hydrate()</code>. The helper runs this callback while persistence is paused, so restoring state does not mark the controller dirty.</td></tr><tr><td><code>autosaveDelayMs?</code></td><td><code>number</code> | <code>null</code></td><td>Debounce delay for autosaves, or <code>null</code> to disable autosave.</td></tr><tr><td><code>version?</code></td><td><code>number</code></td><td>Version returned from <code>storage.getItem()</code> for Zustand persist migrations.</td></tr><tr><td><code>now()?</code></td><td>() =&gt; <code>number</code></td><td>Clock used by the underlying controller, mostly useful for tests.</td></tr><tr><td><code>initialState?</code></td><td><code>TState</code></td><td>Initial state to snapshot when binding <code>store</code> immediately. Useful when the host creates persistence inside a Zustand state creator, before <code>store.getState()</code> returns the completed initial state.</td></tr><tr><td><code>markInitialSnapshotSaved?</code></td><td><code>boolean</code></td><td>Whether an initially-bound store snapshot should be treated as already saved. Defaults to <code>true</code>, which avoids treating the current runtime state as a user edit immediately after binding.</td></tr><tr><td><code>subscribeReason?</code></td><td><a href="./PersistenceSaveReason.html"><code>PersistenceSaveReason</code></a></td><td>Save reason used when <code>bindStore()</code> detects a changed partialized snapshot.</td></tr></tbody></table><h2 id="type-parameters" tabindex="-1">Type Parameters <a class="header-anchor" href="#type-parameters" aria-label="Permalink to &quot;Type Parameters&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Type Parameter</th><th>Default type</th><th>Description</th></tr></thead><tbody><tr><td><code>TState</code></td><td>-</td><td>The full room-store state.</td></tr><tr><td><code>TPersisted</code></td><td><code>Partial</code>&lt;<code>TState</code>&gt;</td><td>The partial state shape written by Zustand persist.</td></tr><tr><td><code>TSnapshot</code></td><td><code>string</code></td><td>The durable snapshot shape handled by the adapter.</td></tr></tbody></table>',10)])])}const u=t(a,[["render",r]]);export{m as __pageData,u as default};
